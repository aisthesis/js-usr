define(function(require) {
    var extend = require('../usr/extend'),
        canvasUtils = require('../usr/draw/canvas-utils'),
        highlights = require('../usr/draw/highlights'),
        Point = require('../usr/draw/point'),
        Shape = require('../usr/draw/shape'),
        Circle = require('../usr/draw/circle'),
        Rectangle = require('../usr/draw/rectangle'),
        RegularPolygon = require('../usr/draw/regular-polygon');
});
